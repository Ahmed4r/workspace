class AppBreakpoints {
  AppBreakpoints._();

  /// Below this, the sidebar collapses into a drawer.
  static const desktop = 850.0;

  /// Below this, dense layouts (tables, metrics row) stack vertically.
  static const mobile = 600.0;
}